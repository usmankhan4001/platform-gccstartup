export function LeadDrawer({ leadId, open, onClose }: { leadId?: string; open: boolean; onClose: () => void }) { if (!open) return null; return <div className="fixed inset-0 z-40 bg-black/50" onClick={onClose}><div className="fixed right-0 top-0 h-full w-full max-w-2xl bg-[var(--bg)] p-6 shadow-lg overflow-y-auto" onClick={e => e.stopPropagation()}><h2 className="text-lg font-bold text-[var(--text)] mb-4">Lead {leadId}</h2><p className="text-[var(--text-secondary)]">Lead details will be connected to the database.</p></div></div> }
