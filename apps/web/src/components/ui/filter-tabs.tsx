export function FilterTabs({ tabs, active, onChange }: { tabs: { id: string; label: string }[]; active: string; onChange: (id: string) => void }) { return <div className="flex gap-1 rounded-lg bg-[var(--bg-secondary)] p-1">{tabs.map(t => <button key={t.id} onClick={() => onChange(t.id)} className={["rounded-md px-3 py-1.5 text-sm font-medium transition-colors", t.id === active ? "bg-[var(--bg)] text-[var(--text)] shadow-sm" : "text-[var(--text-secondary)] hover:text-[var(--text)]"].join(" ")}>{t.label}</button>)}</div> }
