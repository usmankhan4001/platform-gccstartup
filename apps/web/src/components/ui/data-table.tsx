export function DataTable({ columns, data }: { columns: any[]; data: any[] }) { return <div className="rounded-lg border border-[var(--border)] overflow-hidden"><table className="w-full"><thead className="bg-[var(--bg-secondary)]"><tr>{columns.map((c: any) => <th key={c.key} className="px-4 py-3 text-left text-sm font-medium text-[var(--text-secondary)]">{c.label}</th>)}</tr></thead><tbody>{data.map((row: any, i: number) => <tr key={i} className="border-t border-[var(--border)]">{columns.map((c: any) => <td key={c.key} className="px-4 py-3 text-sm text-[var(--text)]">{c.render ? c.render(row) : row[c.key]}</td>)}</tr>)}</tbody></table></div> }
